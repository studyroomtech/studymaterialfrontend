// Admin dashboard Manage Page tab types (Requirements 1.1, 1.2, 1.3, 1.4).
//
// Type declarations only — no logic or values.

/** The identifier of a Manage Page tab. */
export type ManageTabId = 'study-material' | 'test-series';
