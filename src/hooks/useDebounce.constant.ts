// Constants for the debounce hook (Req 4.1).

/**
 * Default debounce delay, in milliseconds, applied to search input so that a
 * search query is only acted upon after the learner stops typing (Req 4.1).
 */
export const DEFAULT_DEBOUNCE_DELAY_MS = 300;
