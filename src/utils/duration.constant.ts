// Constants for the duration formatting utility.

/** Shown when a duration is absent — e.g. per-Section time under Overall Timing. */
export const UNKNOWN_DURATION_LABEL = '—';

/** Shown for a zero-second duration, which reads better than "0s" alongside real times. */
export const ZERO_DURATION_LABEL = 'less than a second';

export const SECONDS_PER_MINUTE = 60;
export const SECONDS_PER_HOUR = 3600;
