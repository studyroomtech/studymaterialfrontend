// Constant values for the PaidMaterialCard component (Requirements 1.16, 12.1, 12.3).
//
// The PaidMaterialCard renders a Paid Material's Price and an entitlement-aware
// action; these labels centralize the user-facing copy for that action.

/** Label for the action shown when the Learner is not yet entitled (Req 12.3). */
export const BUY_ACTION_LABEL = "Buy";

/** Label for the action shown once the Learner holds an Entitlement (Req 12.3). */
export const VIEW_ACTION_LABEL = "View/Download";

/** Accessible prefix announcing the Price of a Paid Material (Req 12.1). */
export const PRICE_LABEL_PREFIX = "Price";
