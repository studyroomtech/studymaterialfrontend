// Constant values for the PercentageBar component.

/** The bar's scale: percentages are always measured against 100. */
export const PERCENTAGE_MAX = 100;

/** The floor of the scale — a bar cannot render a negative fill. */
export const PERCENTAGE_MIN = 0;
