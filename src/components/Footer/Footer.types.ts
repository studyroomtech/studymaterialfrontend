// Types for the Footer component (Requirements 1.15, 1.17).

/** Props accepted by the Footer component. */
export interface FooterProps {
  /** Optional additional class name applied to the footer container. */
  className?: string;
}
